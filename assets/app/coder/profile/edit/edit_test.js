'use strict';

describe('myApp.coder.profile.edit module', function() {

  beforeEach(module('myApp.coder.profile.edit'));

  describe('coder.profile.edit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editProfileCtrl = $controller('EditProfileCtrl');
      expect(editProfileCtrl).toBeDefined();
    }));

  });
});
